import 'package:flutter/material.dart';

class SnakeTail extends StatelessWidget {
  const SnakeTail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
